# Google Drive API設定
# Phase 10-1: ファイル名変更機能のため drive スコープが必要（全ファイルアクセス）
GOOGLE_DRIVE_SCOPES=https://www.googleapis.com/auth/drive
CREDENTIALS_FILE=credentials.json
TOKEN_PATH=token.json

# ファイル管理
PROCESSED_FILE=.processed_drive_files.txt
DOWNLOAD_DIR=downloads

# Webhook設定（時間単位）
CHANNEL_EXPIRATION_HOURS=24

# Gemini API Keys (Free tier vs Paid tier)
# Get your API keys from: https://aistudio.google.com/app/apikey
#
# 無料枠APIキー（必須）
GEMINI_API_KEY_FREE=your_gemini_api_key_here

# 有料枠APIキー（オプション）
GEMINI_API_KEY_PAID=your_gemini_paid_tier_api_key_here

# 有料枠を使う場合は true に設定
USE_PAID_TIER=false

# Phase 10-1: 自動ファイル名変更機能（ローカルのみ）
AUTO_RENAME_FILES=true

# Phase 10-2: 文字起こし完了後、Google Driveファイルは自動削除されます
DELETION_LOG_FILE=.deletion_log.jsonl

# Phase 10-3: iCloud Drive統合（自動検知 + 重複防止）
ENABLE_ICLOUD_MONITORING=true
ICLOUD_DRIVE_PATH=~/Library/Group Containers/group.com.apple.VoiceMemos.shared/Recordings
PROCESSED_FILES_REGISTRY=.processed_files_registry.jsonl
AUTO_DELETE_LOCAL_FILES=false

# Phase 10-4: Google Driveアップロード（新規文字起こしJSONのみ）
ENABLE_DRIVE_UPLOAD=true
DRIVE_UPLOAD_FOLDER=transcriptions
UPLOAD_LOG_FILE=.upload_log.jsonl

# Phase 10-4 拡張: Google Docs出力（モバイルフレンドリー）
ENABLE_DOCS_EXPORT=true

# Phase 11-1: Googleカレンダー連携（予定マッチング + 要約生成統合）
ENABLE_CALENDAR_INTEGRATION=true
CALENDAR_ID=primary

# Phase 11-3: 統合パイプライン（参加者管理 + エンティティ解決 + 話者推論）
ENABLE_INTEGRATED_PIPELINE=true

# Phase 11-4: Vector DB自動構築
ENABLE_VECTOR_DB=true

# Phase 18: CORS設定（カンマ区切りでオリジンを指定）
# 開発環境では localhost、本番環境では実際のドメインを指定
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://your-pwa-domain.com

# Phase 18: API Server設定
API_HOST=0.0.0.0
API_PORT=8001
